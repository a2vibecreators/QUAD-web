# QUAD QA Environment Variables
# Copy this file to .env and fill in real values
# Usage: cp .env.example .env

# NextAuth Configuration
NEXTAUTH_SECRET=<generate-with-openssl-rand-base64-32>
NEXTAUTH_URL=https://qa.quadframe.work

# Google OAuth - Get from Google Cloud Console
# Project: "QUAD Platform" (ID: quad-platform)
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=<your-google-client-id>
GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# GitHub OAuth (optional - if configured)
GITHUB_CLIENT_ID=<your-github-client-id>
GITHUB_CLIENT_SECRET=<your-github-client-secret>

# Java Backend API (Docker network hostname)
# This MUST match the actual container name
QUAD_API_URL=http://quad-services-qa:8080
